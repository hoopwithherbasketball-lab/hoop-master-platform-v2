# MIGRATION_BACKLOG

## Phase 4 migration backlog (planning only)

| Epic | Description | Source paths | Target paths | Risk | Dependencies | Suggested PR size |
|---|---|---|---|---|---|---|
| Public MVP shell stabilization | Finalize public shell, remove placeholder dependencies, normalize nav/auth entry points | `elitegbb` public shell/content paths (pending Phase 3 artifacts) | `apps/web/src/App.tsx`, `apps/web/src/pages/public/*` | Medium | Phase 5 guardrails, Phase 6 tooling | M |
| Page Builder MVP foundation | Define block schema, authoring controls, renderer baseline for admin-managed pages | Legacy references optional; target-owned implementation | `apps/web/src/pages/admin/builder/*` (and optional `packages/page-builder/*`) | Medium | Phase 5 policy checks, Phase 6 runtime | M-L |
| ConnectGBB core migration — slice 1: Supabase auth + member schema | Wire Supabase auth session handling and define member/profile DB schema in target package | `elitegbb` connect/community auth modules (pending artifacts) | `packages/features/src/connectgbb/auth/*`, `packages/supabase/src/schema/member*` | High | Phase 5 guardrails, Supabase/auth mapping | S-M |
| ConnectGBB core migration — slice 2: Member profile routes + pages | Port member profile UI routes, page components, and nav integration | `elitegbb` connect/community profile modules (pending artifacts) | `apps/web/src/pages/connectgbb/profile/*`, `packages/features/src/connectgbb/profile/*` | High | Slice 1 complete, Phase 5 auth guardrails | S-M |
| ConnectGBB core migration — slice 3: Community feed + event pages | Port community feed, event listing, and join-flow pages into target feature package | `elitegbb` community/event modules (pending artifacts) | `apps/web/src/pages/connectgbb/*`, `packages/features/src/connectgbb/community/*` | Medium | Slice 2 complete, Phase 6 runtime | M |
| CRM + forms workflow completion | Migrate or rewrite registration/intake/form workflows with persistence + auditability | `elitegbb` forms/CRM modules (pending artifacts) | `packages/features/src/crm/*`, `apps/web/src/pages/dashboard/*`, `packages/supabase/*` | High | Supabase contracts, role guardrails | M-L |
| Evaluation/scouting workflow migration — slice 1: Data model + DB schema | Define eval schema, scoring rubrics, and Supabase tables for player evaluations | `elitegbb` evaluation/scouting modules (pending artifacts) | `packages/supabase/src/schema/eval*`, `packages/features/src/coaching/schema/*` | High | Phase 5 privacy policy, Supabase contracts | S |
| Evaluation/scouting workflow migration — slice 2: Coach eval UI + submission flow | Port coach-facing eval form, scoring UI, and submission logic | `elitegbb` evaluation UI modules (pending artifacts) | `packages/features/src/coaching/eval/*`, `apps/web/src/pages/coach/eval/*` | High | Slice 1 complete, Phase 5 auth/RBAC guardrails | S-M |
| Evaluation/scouting workflow migration — slice 3: Admin review + privacy gates | Add admin review surface, role-gated visibility controls, and privacy enforcement for eval records | `elitegbb` admin/scouting modules (pending artifacts) | `apps/web/src/pages/admin/evals/*`, `packages/features/src/coaching/admin/*` | High | Slice 2 complete, Phase 5 RBAC mapping, Phase 6 runtime | M |
| Payments hardening (Phase 10) | Add secure checkout session creation, webhook fulfillment, and idempotency handling as MVP-critical scope | Legacy payment hooks/patterns (pending artifacts) | Target payment service path (`apps/api/payments/*` or equivalent) + checkout integration in `apps/web` | High | Deployment/runtime strategy, secrets management, auth/RBAC contracts | M |
| Admin dashboard hardening | Consolidate and migrate remaining admin operational workflows, RBAC checks, and tooling parity from legacy into target admin surfaces | `elitegbb` admin/ops modules (pending artifacts) | `apps/web/src/pages/admin/*`, supporting feature packages in `packages/features/*` | Medium | Auth/RBAC contracts, Supabase policy alignment, Phase 5 guardrails | M |
| Media/TV section rollout | Implement HoopWithHer TV/media route group, content model, and playback integration with privacy-safe publishing defaults | Legacy media/content paths (pending artifacts) + target content specs | `apps/web/src/pages/public/*` (or `apps/web/src/pages/media/*`) + optional media feature package | Medium | CMS/Page Builder decisions, deployment/media hosting contracts | M |
| Deployment contract convergence | Consolidate environment matrix and deployment responsibilities across apps | Legacy deployment/env docs (pending artifacts) | Root deployment docs + CI/CD configs (Phase 6/7 scope) | Medium | Phase 6 command center tooling | S-M |
| Player profiles migration | Port legacy profile models and features into target recruiting/CRM packages | `elitegbb` profile/evaluation paths (pending artifacts) | `packages/features/src/recruiting/*`, `packages/features/src/crm/*`, `apps/web` profile routes | High | Phase 5 policy, auth/RBAC mapping | M |

> **Note (updated Phase 4 patch):** The original `ConnectGBB core migration` and `Evaluation/scouting workflow migration` epics were each marked as a single L-sized PR. Both have been decomposed into 3 focused slices each (S, S-M, or M) to address PR sizing concerns, improve rollback safety, reduce review complexity, and create cleaner sequencing for Phase 5 guardrails and Phase 6 runtime automation. No product app code is changed by this planning doc update.
