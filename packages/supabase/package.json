{
  "name": "@hoop-master/supabase",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "description": "Consolidated Supabase migrations and schema from all repos",
  "dependencies": {
    "@supabase/supabase-js": "^2.105.1"
  }
}
